export interface TotalData {
  confirmed: number;
  recovered: number;
  critical: number;
  deaths: number;
  lastUpdate: Date;
}

export interface CountryData {
  confirmed: number;
  recovered: number;
  critical: number;
  deaths: number;
}
